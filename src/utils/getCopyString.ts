/**
 * Using this function you can make a deep copy of a string.
 *
 * @param str The string to be copied.
 * @returns Copied string.
 */
export function getCopyString(str: string) {
	return (' ' + str).slice(1)
}
